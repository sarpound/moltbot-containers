# =============================================================================
# Moltbot - Environment (copy to .env, set values, never commit .env)
# =============================================================================
# Linux paths only. Use from WSL2 or bare-metal Ubuntu (/opt/moltbot).
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED - Moltbot gateway
# -----------------------------------------------------------------------------
# Gateway token for API/auth. Generate: openssl rand -hex 32
# Ref: https://hub.docker.com/r/moltbot/moltbot
CLAWDBOT_GATEWAY_TOKEN=change-me-use-openssl-rand-hex-32

# -----------------------------------------------------------------------------
# REQUIRED - PostgreSQL (compose stack; not exposed to host)
# -----------------------------------------------------------------------------
POSTGRES_USER=moltbot
POSTGRES_PASSWORD=change-me-use-strong-password
POSTGRES_DB=moltbot

# -----------------------------------------------------------------------------
# REQUIRED - Application port
# -----------------------------------------------------------------------------
# Port Moltbot listens on inside the container. Default 3000.
# Official image may use 18789 (Gateway API); if so set APP_PORT=18789 and
# update Caddyfile reverse_proxy to moltbot:18789.
APP_PORT=18789

# -----------------------------------------------------------------------------
# OPTIONAL - Redis URL (compose stack; not exposed)
# -----------------------------------------------------------------------------
# Passed to Moltbot if it supports Redis. Leave as-is for stack default.
REDIS_URL=redis://redis:6379/0

# -----------------------------------------------------------------------------
# OPTIONAL - Caddy / reverse proxy (for public domain + Let's Encrypt)
# -----------------------------------------------------------------------------
# DOMAIN=agent.example.com
# EMAIL=admin@example.com

# -----------------------------------------------------------------------------
# OPTIONAL - Environment tag (logging / feature flags)
# -----------------------------------------------------------------------------
# ENV=home-prod

# -----------------------------------------------------------------------------
# AI Model Provider Keys
# -----------------------------------------------------------------------------
# AI Model Provider Keys (Optional)
ANTHROPIC_API_KEY=sk-ant-xxx
MOONSHOT_API_KEY=sk-xxx
ZAI_API_KEY=sk-xxx
